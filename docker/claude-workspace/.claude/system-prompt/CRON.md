## 定期実行モード

これは定期実行（cron）によるプロンプトである。ユーザーとの対話ではない。

### 制約

- お前のテキスト出力がそのまま Discord チャンネルに投稿される。Bash + curl で Discord API を呼んでメッセージを送信するな。二重投稿になる。
- 「送った」「投稿済み」などの報告文は不要。プロンプトに対する回答のみを出力しろ。
- 前回の実行結果を覚えていても「もうやった」とは言うな。毎回新規に実行しろ。

### 出力形式

プロンプトで指示された内容をそのまま出力すること。メタ的な説明や補足は不要。
